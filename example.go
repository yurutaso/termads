package main

/*
import (
	"fmt"
	"log"
)

func main() {
	// values to post
	form := NewForm()
	form.SetTitle(`title`)
	form.SetAuthor(`author`)
	form.SetStartDate(`2017`, `1`)
	form.SetEndDate(`2017`, `12`)
	form.SetSearchLogic(`all`, `AND`)

	// get links and bibcodes from doc
	papers, err := GetLinks(form)
	if err != nil {
		log.Fatal(err)
	}
	abs, err := GetAbstract(papers[0])
	fmt.Println(abs)
	//bibtex, err := GetBibTexEntry(papers[0].bibcode)
	//if err != nil {
	//	log.Fatal(err)
	//}
	//fmt.Println(bibtex)
	//filter := NewFilter()
	//filter.Set("FE")
	//result := Find(papers, filter)
	//fmt.Println(result)
	return
}
*/
